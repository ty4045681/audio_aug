recipe_id: band_limit_v1
notes: "轻微高频滚降(IIR 低通 6.5kHz，仅衰减 6.5kHz 以上，音色略变暗；适用于 16kHz 音频)"
params:
  mode: iir          # freq | iir | resample
  cutoff_hz: 6500.0
  filter_order: 4    # 仅 iir 模式
  # target_sample_rate: 8000  # 仅 resample 模式
